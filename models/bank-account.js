class BankAccount {
    constructor(userId, issuer, accountType, balance) {
        this.userId = userId;
        this.issuer = issuer;
        this.accountType = accountType;
        this.balance = balance;
    }
}
